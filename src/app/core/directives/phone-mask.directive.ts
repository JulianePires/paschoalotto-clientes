// Phone mask directive removed in favor of ngx-mask.
// Keep a small placeholder to avoid build issues while migrating.
// REMOVED: phone-mask directive replaced by ngx-mask integration.
// File intentionally left blank to avoid build issues; safe to delete locally if you prefer.

